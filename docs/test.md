# Test

Run from project root:

    build/test.sh

Runs the full pytest test suite with verbose output. Pass additional pytest arguments after the command, e.g.:

    build/test.sh tests/midi/ -k test_name
